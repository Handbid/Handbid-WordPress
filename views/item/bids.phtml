<?php

if ($this->get('item')) {

    $item       = $this->get('item');
    $bids       = $this->get('bids');
    $profile    = $this->get('profile');

} else {
    echo "No item found.";
    return;
}

if(count($bids) > 0) {
    ?>

    <ul class="simple-list bid-history">
        <?php
        foreach ($bids as $bid) {
            if($bid->amount){
            ?>
            <li class="row <?php echo ($bid->status == 'winning') ? 'open' : 'closed'; ?>">
                <div class="col-md-6">
                    <h4>Bidder Pin</h4>
                    <span><?php echo $bid->bidderName; ?></span>
                </div>
                <div class="col-md-6">
                    <span class="bid-amount winning">$<span><?php echo $bid->amount; ?></span></span>
                </div>
            </li>
        <?php
            }
        }
        ?>
    </ul>
<?php
}
else
{
    ?>
    <p class="center-text">This item has no bids.</p>
<?php
}
?>