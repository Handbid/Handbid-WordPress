<?php
if (isset($this->context['item']) || $this->context) {
    $item = $this->context['item'];
} else {
    echo "No Item Set";
    return;
}

var_dump($item); exit;
?>

<div class="widget widget-winningbidder widgetContainer">
    <div class="header"><?php echo __('Winning bid'); ?></div>
    <div class="amount"><?php echo '$' . $item->highestBid->amount; ?></div>
    <div class="alias"><?php echo __('By' . ' ' . $item['highestBid']['_restMetaData']['bidderName'] . ' ( ' . $item['highestBid']['_restMetaData']['bidderPin'] . ' )'); ?></div>
    <div class="original-value">
        <div class="original-text"><?php echo __('Original Value'); ?></div>
        <div class="original-amount"><?php echo '$' . $item->startingPrice; ?></div>
    </div>
</div>