<?php
if ($this->get('item')) {
    $item = $this->get('item');
} else {
    echo "No Item Set";
    return;
}
?>

<div class="product-info">
    <h1><?php echo $item->name; ?></h1>
    <div class="description">
        <div>
            <?php if(isset($item->donor) || $item->donor) {
                ?>
                <span><?php echo __('Donated by') . ':'; ?></span> <?php echo $item->donor; ?>
            <?php
            }
            ?>
        </div>
        <div>
            <span><?php echo __('Minimum next bid') . ':'; ?></span> <?php echo $item->minimumBidAmount; ?>
        </div>
        <div>
            <span><?php echo __('Bid increments by') . ':'; ?></span> <?php echo $item->bidIncrement; ?>
        </div>
    </div>
    <div class="product-extra clearfix">
        <div class="quantity-adder">
            <span class="add-up add-action button">+</span>
            <input class="form-control" type="text" name="quantity" size="2" value="<?php echo $item->minimumBidAmount; ?>">
            <span class="add-down add-action button">-</span>
        </div>
        <button type="submit" class="bidNow button"><?php echo __('Bid Now'); ?></button>
        <button type="submit" id="proxyBid" class="proxyBid button"><?php echo __('Make a Proxy Bid'); ?></button>
    </div>
</div>