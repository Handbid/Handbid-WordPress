<?php
if ($this->get('item')) {
    $item = $this->get('item');
} else {
    echo "No Item Set";
    return;
}
?>
<div class="product-info">
    <ul class="description dividedList">
        <?php if(isset($item->donor) || $item->donor) {
            ?>
            <li>
                <span><?php echo __('Donated by') . ':'; ?></span><span><?php echo $item->donor; ?></span>
            </li>
        <?php
        }
        ?>
        <?php if(isset($item->closingTime) || $item->closingTime) {
            ?>
            <li>
                <span><?php echo __('Closing Time') . ':'; ?></span><span><?php echo date('g:i A m/d/Y', $item->closingTime); ?></span>
            </li>
        <?php
        }
        ?>
        <?php if(isset($item->highestBid) || $item->highestBid) {
            ?>
            <li>
                <span><?php echo __('Current Bid') . ':'; ?></span><span>$<?php echo $item->highestBid; ?></span>
            </li>
        <?php
        }
        ?>
        <li>
            <div class="bid-adder bidUpDown">
                <span class="add-up up add-action button">+</span>
                <span class="add-down down add-action button">-</span>
            </div>
            <p class="bid-value bidAmountTotal"><?php echo $item->minimumBidAmount; ?></p>
            <div class="bidBar">
                <button type="submit" class="bidNow button"><?php echo __('Bid'); ?></button>
                <button type="submit" class="proxyBid button"><?php echo __('Max Bid'); ?></button>
            </div>
        </li>
        <li>
            <span><?php echo __('Bid increment') . ':'; ?></span><span class="bidIncrement">$<?php echo $item->bidIncrement; ?></span>
        </li>
    </ul>

</div>