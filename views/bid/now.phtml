<?php
if ($this->context[0]) {
    $item = $this->context[0];
} else {
    echo "No Item Set";
    return;
}
?>

<div class="biddingContainer">
    <div class="bidDescription leftContainer">
        <div class="label" id="bidLabel"><?= __('Minimum next bid'); ?></div>
        <div class="amount">
            $<span class="value" id="bidAmountTotal"><?= $item['minimumBidAmount']; ?></span>
        </div>
        <button class="reset"><?= __('Reset Bid'); ?></button>
        <div class="bidIncrement">
            <?= __('Bid increments by') . ' $'; ?><span class="value"><?= $item['bidIncrement']; ?></span>
        </div>
    </div>
    <div class="bidAction rightContainer">
        <div class="bidUpDown">
            <button class="down"><?= __('Down'); ?></button><button class="up"><?= __('Up'); ?></button>
        </div>
        <button class="bidNow"><?= __('Bid Now'); ?></button>
        <button class="proxy" id="proxyBid"><?= __('Make a Proxy Bid'); ?></button>
    </div>
</div>
