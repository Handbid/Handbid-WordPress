<?php
if ($this->context['organization']) {
    $org = $this->context['organization'];
} else {
    echo "No Organization Set";
    return;
}
?>
<header id="title-area" class="clearfix headerCover"><div class="inner" style="opacity: 1;"><h1 class="page-title entry-title"><?= $org->name; ?></h1></div></header>

<div class="organizationDetailsContainer">
    <img src="<?= $org->logo; ?>"/>
    <div class="orgDetails">
        <div class="address">
            <?= $org->address->full; ?>
        </div>
        <div class="website">
            <a href="<?= $org->website; ?>" target="_blank"><?= $org->website; ?></a>
        </div>
        <div class="description">
            <?= $org->description; ?>
        </div>
    </div>
</div>