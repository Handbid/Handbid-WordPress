<?php
if ($this->context[0]) {
    $auction = $this->context[0];
} else {
    echo "No Auction Set";
    return;
}
?>

<div class="eventsAndDetails">
    <div class="sectionHeading"><?= __('Auction & Event Details'); ?></div>
    <div class="socialLinks">
        <a href="#">Facebook</a>
        <a href="#">Twitter</a>
        <a href="#">Share</a>
    </div>
    <div class="detailsWrapper">
        <ul class="eventDetails">
            <li><label>Event Dates: </label><?= $auction['startTime']; ?> - <?= $auction['endTime']; ?></li>
            <li><label>Event Location: </label><?= $auction['vanityAddress']; ?></li>
            <li><label>Event Address: </label><?= $auction['location']; ?></li>
        </ul>

        <div class="actionBar">
            <a href="#" class="register"><?= __('Register'); ?></a>
            <a href="#" class="buyTickets"><?= __('Buy Tickets'); ?></a>
        </div>
    </div>

</div>