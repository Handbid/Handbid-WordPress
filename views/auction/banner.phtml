<?php

$auction        = $this->get('auction');
$location       = $auction->location;

$winningBids    = $this->get('winningBids');
$losingBids     = $this->get('losingBids');

$profile        = $this->get('profile');

if (isset($auction->meta)) {
    $org = $auction->meta->organization;
}


if (!$auction) {
    echo "No Auction Set";
    return;
}

$status = ($auction->status == "open") ? 'open' : 'closed';
$url    = get_permalink() . '?auction=' . get_query_var('auction');

?>

<div class="section-title">
    <h1 class="title">
        <?php echo $auction->name; ?>
        <span class="auctionDateTime">
            <?php echo date('h:i a m/d/Y', $auction->startTime); ?>
        </span>
    </h1>
</div>

<div class="bannerContainer">
    <div class="innerBanner">
        <div class="section-title statBar">

            <div class="innerStatBar">

                <?php if ($profile) { ?>

                    <div class="statBox winning">
                        <label><?php echo __('Winning'); ?></label>
                        <span><?php echo $winningBids; ?></span>
                    </div>

                    <div class="statBox losing">
                        <label><?php echo __('Losing'); ?></label>
                        <span><?php echo $losingBids; ?></span>
                    </div>

                    <div class="statBox proxy">
                        <label><?php echo __('Proxy'); ?></label>
                        <span><?php echo $this->get('proxies'); ?></span>
                    </div>

                <?php } ?>

                <div class="statBox statusBox auction-<?php echo $status; ?>">
                    <label><?php echo __(ucfirst($status)); ?></label>
                </div>
                <?php if (!$profile) {

                    ?>
                    <a class="handbid-connect" href="#" data-handbid-connect></a>
                <?php
                }
                else
                {
                    ?>
                    <div class="statBox bidderProfile">
                        <label><?php echo $profile->name ?></label>
                        <span><?php echo 'Pin # ' . $profile->pin; ?></span>
                    </div>
                <?php
                }
                ?>
            </div>
        </div>
    </div>
</div>
